const Layout = ( {children} ) => {
    return (
        <div className="h-screen space-y-3 py-20 padding flex flex-col items-center mt-20 bg-blue-100">
            
            {children}
        </div>
    )
}

export default Layout 